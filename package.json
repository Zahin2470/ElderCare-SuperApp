{
  "name": "eldercare-superapp",
  "private": true,
  "version": "0.2.0",
  "description": "ElderCare SuperApp — monorepo (frontend: React/Vite, backend: Express/PostgreSQL). Convenience scripts only; each folder is an independent npm project.",
  "scripts": {
    "install:all": "npm --prefix backend ci && npm --prefix frontend ci",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "db:migrate": "npm --prefix backend run migrate",
    "db:seed": "npm --prefix backend run seed",
    "typecheck": "npm --prefix backend run typecheck && npm --prefix frontend run typecheck",
    "test": "npm --prefix backend test && npm --prefix frontend test",
    "build": "npm --prefix backend run build && npm --prefix frontend run build"
  }
}
